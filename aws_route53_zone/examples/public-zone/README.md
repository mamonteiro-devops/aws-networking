# Route53 Public Zone Example

This example create 1 public hosted zone.  

## Usage

To run this example you need to execute:

```bash
$ terraform init
$ terraform plan
$ terraform apply
```

Run `terraform destroy` to remove these resources.
